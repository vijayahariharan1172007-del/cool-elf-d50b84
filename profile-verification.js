(()=>{const $=id=>document.getElementById(id);const key='exc_portal_access';const existing=(()=>{try{return JSON.parse(localStorage.getItem(key)||'null')}catch{return null}})();if(existing?.accessToken&&Date.now()<Number(existing.expiresAt||0)){location.replace('profile.html');return;}const api=async(body)=>{const r=await fetch('/api/user-access',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(body),cache:'no-store'});const t=await r.text();let j={};try{j=JSON.parse(t)}catch{}if(!r.ok||j.ok===false)throw Error(j.error||(t&&t.slice(0,240))||`HTTP ${r.status}`);return j};const normalizeId=v=>{let s=String(v??'').normalize('NFKC').trim().toUpperCase().replace(/[^A-Z0-9]/g,'');if(/^EX26\d{1,6}$/.test(s))s='EX26'+s.slice(4);if(/^EX26\d{1,6}$/.test(s))s='EX26-'+s.slice(4).padStart(6,'0');return s};const formatId=v=>{let s=String(v??'').normalize('NFKC').toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,10);if(s.startsWith('EX26')){const n=s.slice(4).replace(/\D/g,'').slice(0,6);return n?'EX26-'+n:'EX26'}return s};const id=$('profileVerifyMaster'),phone=$('profileVerifyMobile'),btn=$('profileVerifyBtn'),status=$('profileVerifyStatus');id.focus();id.addEventListener('input',e=>e.target.value=formatId(e.target.value));phone.addEventListener('input',e=>e.target.value=e.target.value.replace(/\D/g,'').slice(0,10));const verify=async()=>{const masterId=normalizeId(id.value),mobile=phone.value.replace(/\D/g,'').slice(-10);id.value=masterId;phone.value=mobile;if(!/^EX26-\d{6}$/.test(masterId)||mobile.length!==10){status.textContent='ENTER A VALID MASTER ID (EX26-XXXXXX) AND 10-DIGIT REGISTERED MOBILE NUMBER';return}btn.disabled=true;status.textContent='VERIFYING PROFILE ACCESS…';try{const r=await api({masterId,phone:mobile});if(!r.accessToken||!r.master)throw Error('PROFILE ACCESS COULD NOT BE CREATED.');localStorage.setItem(key,JSON.stringify({accessToken:r.accessToken,master:r.master,expiresAt:Number(r.sessionExpiresAt||Date.now()+1800000)}));status.textContent='VERIFIED • OPENING PROFILE…';location.replace('profile.html')}catch(e){status.textContent=e.message||'INVALID MASTER ID OR REGISTERED MOBILE NUMBER';btn.disabled=false}};btn.onclick=verify;[id,phone].forEach(input=>input.addEventListener('keydown',e=>{if(e.key==='Enter')verify()}));const panel=$('jarvisPanel');$('jarvisBtn').onclick=()=>{panel.classList.add('open');panel.setAttribute('aria-hidden','false')};$('jarvisClose').onclick=()=>{panel.classList.remove('open');panel.setAttribute('aria-hidden','true')};panel.addEventListener('click',e=>{if(e.target===panel)$('jarvisClose').click()});const routes={HOME:'site.html',REGISTRATIONS:'registration-portals.html',PROFILE:'profile.html','GENERAL RULES':'general-rules.html',CONTACTS:'contact.html'};document.querySelectorAll('.jarvis-links button').forEach(b=>b.onclick=()=>{const t=b.dataset.target;if(routes[t])location.href=routes[t]})})();